import { InjectionToken } from '@angular/core';

export const FS_APP_MESSAGE_ROOT_CONFIG = new InjectionToken<any[]>('fs-app-message-root-config');
export const FS_APP_MESSAGE_CONFIG = new InjectionToken<any[]>('fs-app-message-config');
